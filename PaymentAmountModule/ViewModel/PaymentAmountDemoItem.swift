#if DEBUG_MENU

/// Represents selectable items in PaymentAmountModule
enum PaymentAmountDemoItem: String {

    /// Represents `PaymentAmount.Amount` text field cell
    case amount = "Amount"

    /// Represents `PaymentAmount.DiscountedAmount` text field cell
    case discountedAmount = "Discounted amount"

    /// Represents `PaymentAmount.Comment` text field cell
    case comment = "Comment"

    /// Represents `PaymentAmount.Hint` text field cell
    case hint = "Hint"
}

#endif
