#if DEBUG_MENU

/// Represents selectable items in AccountSelectorModule
enum AccountSelectorDemoItem: String {

    /// Represents `AccountSelector.Title` text field cell
    case title = "Title"

    /// Represents `AccountSelector.AccountName` text field cell
    case accountName = "Account name"

    /// Represents `AccountSelector.Amount` text field cell
    case amount = "Amount"

    /// Represents `AccountSelector.AccountNumber` text field cell
    case accountNumber = "Account number"
}

#endif
